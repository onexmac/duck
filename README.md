# duck

A simple Q&A CLI that answers your questions using Claude AI.

## Setup

```bash
pip install -r requirements.txt
export ANTHROPIC_API_KEY=your-api-key
```

## Usage

```bash
python main.py
```

Then type any question and press Enter. Type `quit` or `exit` to stop.